# Versions corrigées et TESTÉES pour compatibilité - Python 3.13 compatible (RÉSOLUTION CONFLIT)

# Core FastAPI - Versions optimisées
fastapi==0.112.2
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
httpx==0.27.2
httpcore==1.0.5

# LlamaIndex versions COMPATIBLES - Résolution du conflit de dépendances
llama-index-core==0.13.0
llama-index-vector-stores-weaviate==1.4.1
llama-index-embeddings-openai==0.5.1
llama-index-llms-openai==0.5.1

# Weaviate Client v3 (DERNIÈRE VERSION STABLE v3)
weaviate-client==3.26.7

# Intelligence métier préservée - Python 3.13 compatible
langdetect==1.0.9
transformers==4.46.0  # Version compatible Python 3.13
torch>=2.0.0  # Version compatible Python 3.13

# APIs externes - VERSION COMPATIBLE avec LlamaIndex
openai==1.42.0  # Version mise à jour pour compatibilité

# VoyageAI pour reranking amélioré (optionnel mais recommandé)
voyageai==0.3.5

# Cross-encoder pour reranking multi-stage (optionnel)
sentence-transformers==3.3.1  # Version compatible Python 3.13

# Utilitaires optimisés
numpy>=1.24.0  # Compatible Python 3.13
scipy>=1.11.0  # Compatible Python 3.13

# Monitoring et métriques (optionnel)
prometheus-client==0.20.0

# Outils de debugging et logging amélioré
structlog==23.2.0

# Support pour formats de fichiers étendus (si nécessaire)
pandas>=2.0.0  # Compatible Python 3.13
openpyxl==3.1.2

# Sécurité et validation améliorée
pydantic>=2.8.0,<3.0.0
email-validator==2.1.0

# Cache et performance (optionnel)
redis==5.0.1
hiredis==2.3.2

# Tests et qualité (développement)
pytest==8.3.3  # Version
The key changes are updating llama-index-embeddings-openai and llama-index-llms-openai to 0.5.1, which is compatible with llama-index-core==0.13.0 and llama-index-vector-stores-weaviate==1.4.1.