name: Ultra Fast Build and Deploy

on:
  push:
    branches: [ main ]
    paths-ignore:
      - '**.md'
      - '.gitignore'
      - 'docs/**'
      - 'README.md'
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # 🔍 Détecter les changements
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      frontend-changed: ${{ steps.changes.outputs.frontend }}
      backend-changed: ${{ steps.changes.outputs.backend }}
      llm-changed: ${{ steps.changes.outputs.llm }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: dorny/paths-filter@v3
        id: changes
        with:
          filters: |
            frontend:
              - 'frontend/**'
            backend:
              - 'backend/**'
            llm:
              - 'llm/**'

  # 🔥 BUILDS PARALLÈLES + NETTOYAGE INTÉGRÉ AMÉLIORÉ
  build-frontend:
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.frontend-changed == 'true'
    steps:
      - uses: actions/checkout@v4

      - name: Install doctl + Cleanup AVANT build
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DOCR_ACCESS_TOKEN }}

      - name: ⚡ Nettoyage INTELLIGENT Frontend (AMÉLIORÉ)
        run: |
          echo "🔥 Nettoyage intelligent intelia-frontend..."
          
          # Vérification préliminaire du repository
          if ! doctl registry repository list | grep -q "intelia-frontend"; then
            echo "ℹ️ Repository intelia-frontend n'existe pas encore"
            exit 0
          fi
          
          # Récupération des manifests avec retry et format amélioré
          max_retries=3
          retry_count=0
          manifests=""
          
          while [ $retry_count -lt $max_retries ]; do
            # Format amélioré pour obtenir plus d'informations
            if manifests=$(doctl registry repository list-manifests intelia-frontend --format Digest,UpdatedAt,Tags,CompressedSize --no-header 2>/dev/null); then
              break
            fi
            retry_count=$((retry_count + 1))
            echo "⏳ Tentative $retry_count/$max_retries..."
            sleep 3
          done
          
          if [ -z "$manifests" ]; then
            echo "ℹ️ Aucune image trouvée ou repository vide"
            exit 0
          fi
          
          total=$(echo "$manifests" | wc -l)
          echo "📊 Images trouvées: $total"
          
          if [ "$total" -le 5 ]; then
            echo "✅ Nettoyage non nécessaire ($total <= 5 images)"
            exit 0
          fi
          
          # Stratégie de nettoyage plus conservatrice
          to_keep=3  # Garder seulement les 3 plus récentes
          to_delete=$((total - to_keep))
          echo "🎯 Suppression de $to_delete anciennes images (garde les $to_keep plus récentes)..."
          
          # Tri par date et suppression avec vérifications avancées
          deleted_count=0
          failed_count=0
          skipped_count=0
          
          echo "$manifests" | sort -k2,2 | head -n "$to_delete" | while read digest date tags size; do
            if [ -n "$digest" ] && [[ "$digest" == sha256:* ]]; then
              # Vérifier si l'image a des tags (images taguées sont plus susceptibles d'être verrouillées)
              if [ -n "$tags" ] && [ "$tags" != "<none>" ]; then
                echo "⚠️ Ignorer (taguée): ${digest:0:20}... tags: $tags"
                skipped_count=$((skipped_count + 1))
                continue
              fi
              
              echo "🗑️ Tentative suppression: ${digest:0:20}... (${date}) [${size}]"
              
              # Double vérification que l'image existe encore
              if doctl registry repository list-manifests intelia-frontend --format Digest --no-header 2>/dev/null | grep -q "$digest"; then
                # Tentative de suppression avec timeout
                if timeout 30 doctl registry repository delete-manifest intelia-frontend "$digest" --force 2>/dev/null; then
                  echo "✅ Supprimé: ${digest:0:20}..."
                  deleted_count=$((deleted_count + 1))
                else
                  echo "⚠️ Échec: ${digest:0:20}... (verrouillé ou en cours d'utilisation)"
                  failed_count=$((failed_count + 1))
                fi
              else
                echo "ℹ️ Déjà supprimé: ${digest:0:20}..."
              fi
              
              # Délai progressif pour éviter le rate limiting
              sleep 2
            fi
          done
          
          echo "📈 Résumé: $deleted_count supprimés, $failed_count échecs, $skipped_count ignorés (taguées)"
          
          # Si beaucoup d'échecs, programmer un nettoyage différé
          if [ "$failed_count" -gt 5 ]; then
            echo "🔄 Trop d'échecs détectés. Programmation d'un nettoyage différé recommandée."
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DOCR
        uses: docker/login-action@v3
        with:
          registry: registry.digitalocean.com
          username: doctl
          password: ${{ secrets.DOCR_ACCESS_TOKEN }}

      - name: 🚀 Build & push frontend (OPTIMISÉ)
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          push: true
          tags: |
            registry.digitalocean.com/${{ secrets.DOCR_NAME }}/intelia-frontend:main-${{ github.sha }}
            registry.digitalocean.com/${{ secrets.DOCR_NAME }}/intelia-frontend:main
          cache-from: |
            type=registry,ref=registry.digitalocean.com/${{ secrets.DOCR_NAME }}/intelia-frontend:buildcache
            type=registry,ref=registry.digitalocean.com/${{ secrets.DOCR_NAME }}/intelia-frontend:main
          cache-to: type=registry,ref=registry.digitalocean.com/${{ secrets.DOCR_NAME }}/intelia-frontend:buildcache,mode=max
          build-args: |
            NEXT_PUBLIC_SUPABASE_URL=${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
            NEXT_PUBLIC_SUPABASE_ANON_KEY=${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
            LLM_BACKEND_URL=${{ secrets.LLM_BACKEND_URL }}
            BUILDKIT_INLINE_CACHE=1
          platforms: linux/amd64
          provenance: false
          sbom: false
          outputs: type=registry,compression=zstd,compression-level=3

  # 🧹 NETTOYAGE DIFFÉRÉ (job séparé pour les images verrouillées)
  deferred-cleanup:
    runs-on: ubuntu-latest
    needs: [build-frontend, build-backend, build-llm]
    if: always() && (needs.build-frontend.result == 'success' || needs.build-backend.result == 'success' || needs.build-llm.result == 'success')
    steps:
      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DOCR_ACCESS_TOKEN }}

      - name: 🧹 Nettoyage différé des images non-taguées
        run: |
          echo "🧹 NETTOYAGE DIFFÉRÉ DES IMAGES NON-TAGUÉES"
          
          for repo in intelia-frontend intelia-backend intelia-llm; do
            echo "🔍 Analyse $repo pour images non-taguées..."
            
            if manifests=$(doctl registry repository list-manifests "$repo" --format Digest,UpdatedAt,Tags --no-header 2>/dev/null); then
              if [ -n "$manifests" ]; then
                # Cibler spécifiquement les images sans tags
                untagged=$(echo "$manifests" | grep -E '\s+<none>\s*$' | head -10)
                
                if [ -n "$untagged" ]; then
                  echo "🎯 Images non-taguées trouvées dans $repo:"
                  
                  echo "$untagged" | while read digest date tags; do
                    echo "🗑️ Suppression image non-taguée: ${digest:0:20}..."
                    
                    if timeout 20 doctl registry repository delete-manifest "$repo" "$digest" --force 2>/dev/null; then
                      echo "✅ Supprimé: ${digest:0:20}..."
                    else
                      echo "⚠️ Échec différé: ${digest:0:20}..."
                    fi
                    
                    sleep 1
                  done
                else
                  echo "ℹ️ Aucune image non-taguée dans $repo"
                fi
              fi
            else
              echo "⚠️ $repo: impossible de lister les manifests"
            fi
            
            sleep 2
          done

      - name: 🗑️ Garbage Collection avec retry
        run: |
          echo "🗑️ Lancement garbage collection avec retry..."
          
          max_attempts=3
          attempt=1
          
          while [ $attempt -le $max_attempts ]; do
            echo "Tentative $attempt/$max_attempts..."
            
            if doctl registry garbage-collection start ${{ secrets.DOCR_NAME }} --force 2>/dev/null; then
              echo "✅ Garbage collection lancée avec succès"
              break
            else
              echo "⚠️ Échec tentative $attempt"
              
              if [ $attempt -eq $max_attempts ]; then
                echo "❌ Garbage collection échouée après $max_attempts tentatives"
              else
                echo "⏳ Attente avant retry..."
                sleep 30
              fi
            fi
            
            attempt=$((attempt + 1))
          done

  # 🔄 NETTOYAGE PROGRAMMÉ (optionnel, via workflow_dispatch)
  scheduled-cleanup:
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    steps:
      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DOCR_ACCESS_TOKEN }}

      - name: 🔄 Nettoyage en profondeur programmé
        run: |
          echo "🔄 NETTOYAGE EN PROFONDEUR PROGRAMMÉ"
          
          for repo in intelia-frontend intelia-backend intelia-llm; do
            echo "🧹 Nettoyage en profondeur de $repo..."
            
            # Attendre un peu pour que les conteneurs se libèrent
            sleep 10
            
            if manifests=$(doctl registry repository list-manifests "$repo" --format Digest,UpdatedAt,Tags --no-header 2>/dev/null); then
              total=$(echo "$manifests" | wc -l)
              
              if [ "$total" -gt 2 ]; then
                to_delete=$((total - 2))  # Garder seulement les 2 plus récentes
                echo "🎯 Suppression agressive: $to_delete images de $repo"
                
                echo "$manifests" | sort -k2,2 | head -n "$to_delete" | while read digest date tags; do
                  if [ -n "$digest" ]; then
                    echo "🗑️ Suppression forcée: ${digest:0:20}..."
                    
                    # Plusieurs tentatives avec délais croissants
                    for retry in 1 2 3; do
                      if timeout 60 doctl registry repository delete-manifest "$repo" "$digest" --force 2>/dev/null; then
                        echo "✅ Supprimé (tentative $retry): ${digest:0:20}..."
                        break
                      else
                        echo "⚠️ Échec tentative $retry/3"
                        if [ $retry -lt 3 ]; then
                          sleep $((retry * 10))
                        fi
                      fi
                    done
                  fi
                  
                  sleep 3
                done
              fi
            fi
          done
          
          echo "🔄 Nettoyage programmé terminé"

  # Les autres jobs restent identiques...
  build-backend:
    # [Le reste du job build-backend avec le même pattern amélioré]
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.backend-changed == 'true'
    steps:
      - uses: actions/checkout@v4
      # [Répliquer la logique améliorée ici]

  build-llm:
    # [Le reste du job build-llm avec le même pattern amélioré]
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.llm-changed == 'true'
    steps:
      - uses: actions/checkout@v4
      # [Répliquer la logique améliorée ici]

  # 📊 Résumé INSTANTANÉ
  summary:
    runs-on: ubuntu-latest
    needs: [detect-changes, build-frontend, build-backend, build-llm, deferred-cleanup]
    if: always()
    steps:
      - name: ⚡ RAPPORT ULTRA RAPIDE
        run: |
          echo "🚀 DÉPLOIEMENT ULTRA RAPIDE TERMINÉ"
          echo "==================================="
          
          # Statuts
          if [[ "${{ needs.detect-changes.outputs.frontend-changed }}" == "true" ]]; then
            echo "Frontend: ${{ needs.build-frontend.result == 'success' && '✅ DÉPLOYÉ' || '❌ ÉCHEC' }}"
          else
            echo "Frontend: ⏸️ INCHANGÉ"
          fi
          
          if [[ "${{ needs.detect-changes.outputs.backend-changed }}" == "true" ]]; then
            echo "Backend: ${{ needs.build-backend.result == 'success' && '✅ DÉPLOYÉ' || '❌ ÉCHEC' }}"
          else
            echo "Backend: ⏸️ INCHANGÉ"
          fi
          
          if [[ "${{ needs.detect-changes.outputs.llm-changed }}" == "true" ]]; then
            echo "LLM: ${{ needs.build-llm.result == 'success' && '✅ DÉPLOYÉ' || '❌ ÉCHEC' }}"
          else
            echo "LLM: ⏸️ INCHANGÉ"
          fi
          
          echo ""
          echo "⚡ OPTIMISATIONS APPLIQUÉES:"
          echo "• Builds 100% parallèles"
          echo "• Nettoyage intelligent multi-phase"
          echo "• Stratégie différée pour images verrouillées"
          echo "• Cache optimisé + compression zstd"
          echo "• Métadonnées désactivées (provenance/sbom)"
          echo "• Timeouts et retry logic"
          echo "• Ciblage des images non-taguées"
          echo "• Garbage collection robuste"