name: Simple Build and Deploy

on:
  push:
    branches: [ main ]
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Détection des changements
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      frontend-changed: ${{ steps.changes.outputs.frontend }}
      backend-changed: ${{ steps.changes.outputs.backend }}
      llm-changed: ${{ steps.changes.outputs.llm }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: dorny/paths-filter@v3
        id: changes
        with:
          filters: |
            frontend:
              - 'frontend/**'
            backend:
              - 'backend/**'
            llm:
              - 'llm/**'

  # Build Frontend
  build-frontend:
    runs-on: ubuntu-latest
    needs: detect-changes
    if: always() && (needs.detect-changes.outputs.frontend-changed == 'true' || needs.detect-changes.outputs.backend-changed == 'true' || needs.detect-changes.outputs.llm-changed == 'true')
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DOCR
        uses: docker/login-action@v3
        with:
          registry: registry.digitalocean.com
          username: doctl
          password: ${{ secrets.DOCR_ACCESS_TOKEN }}

      - name: Build and push frontend
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          push: true
          tags: |
            registry.digitalocean.com/${{ secrets.DOCR_NAME }}/intelia-frontend:latest
            registry.digitalocean.com/${{ secrets.DOCR_NAME }}/intelia-frontend:${{ github.sha }}
          cache-from: type=registry,ref=registry.digitalocean.com/${{ secrets.DOCR_NAME }}/intelia-frontend:buildcache
          cache-to: type=registry,ref=registry.digitalocean.com/${{ secrets.DOCR_NAME }}/intelia-frontend:buildcache,mode=max
          platforms: linux/amd64
          provenance: false
          build-args: |
            NEXT_PUBLIC_SUPABASE_URL=${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
            NEXT_PUBLIC_SUPABASE_ANON_KEY=${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
            NEXT_PUBLIC_API_BASE_URL=https://expert.intelia.com/api
            NEXT_PUBLIC_API_URL=https://expert.intelia.com/api

  # Build Backend
  build-backend:
    runs-on: ubuntu-latest
    needs: detect-changes
    if: always() && (needs.detect-changes.outputs.frontend-changed == 'true' || needs.detect-changes.outputs.backend-changed == 'true' || needs.detect-changes.outputs.llm-changed == 'true')
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DOCR
        uses: docker/login-action@v3
        with:
          registry: registry.digitalocean.com
          username: doctl
          password: ${{ secrets.DOCR_ACCESS_TOKEN }}

      - name: Build and push backend
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          push: true
          tags: |
            registry.digitalocean.com/${{ secrets.DOCR_NAME }}/intelia-backend:latest
            registry.digitalocean.com/${{ secrets.DOCR_NAME }}/intelia-backend:${{ github.sha }}
          cache-from: type=registry,ref=registry.digitalocean.com/${{ secrets.DOCR_NAME }}/intelia-backend:buildcache
          cache-to: type=registry,ref=registry.digitalocean.com/${{ secrets.DOCR_NAME }}/intelia-backend:buildcache,mode=max
          platforms: linux/amd64
          provenance: false

  # Build LLM
  build-llm:
    runs-on: ubuntu-latest
    needs: detect-changes
    if: always() && (needs.detect-changes.outputs.frontend-changed == 'true' || needs.detect-changes.outputs.backend-changed == 'true' || needs.detect-changes.outputs.llm-changed == 'true')
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DOCR
        uses: docker/login-action@v3
        with:
          registry: registry.digitalocean.com
          username: doctl
          password: ${{ secrets.DOCR_ACCESS_TOKEN }}

      - name: Build and push LLM
        uses: docker/build-push-action@v6
        with:
          context: ./llm
          push: true
          tags: |
            registry.digitalocean.com/${{ secrets.DOCR_NAME }}/intelia-llm:latest
            registry.digitalocean.com/${{ secrets.DOCR_NAME }}/intelia-llm:${{ github.sha }}
          cache-from: type=registry,ref=registry.digitalocean.com/${{ secrets.DOCR_NAME }}/intelia-llm:buildcache
          cache-to: type=registry,ref=registry.digitalocean.com/${{ secrets.DOCR_NAME }}/intelia-llm:buildcache,mode=max
          platforms: linux/amd64
          provenance: false

  # NOUVEAU JOB: Déploiement automatique
  deploy:
    name: Deploy to DigitalOcean App Platform
    runs-on: ubuntu-latest
    needs: [build-frontend, build-backend, build-llm]
    if: ${{ always() && (needs.build-frontend.result == 'success' || needs.build-backend.result == 'success' || needs.build-llm.result == 'success') }}
    steps:
      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DOCR_ACCESS_TOKEN }}

      # Diagnostic complet avant déploiement
      - name: Debug App Platform configuration
        run: |
          echo "🔍 Diagnostic de la configuration App Platform..."
          
          # Récupérer la spec actuelle de l'app
          echo "📋 Récupération de la spec actuelle..."
          doctl apps spec get ${{ secrets.DO_APP_ID }} > current-app-spec.yaml
          
          echo "📝 Configuration actuelle de l'app:"
          cat current-app-spec.yaml
          
          echo ""
          echo "🔍 Images configurées dans App Platform:"
          # Extraire les images de la spec
          grep -A 2 -B 2 "image:" current-app-spec.yaml || echo "Aucune image trouvée dans la spec"
          
          echo ""
          echo "📦 Images disponibles dans le registry:"
          
          # Lister toutes les images du registry
          echo "Backend images:"
          doctl registry repository list-manifests intelia-backend --format Tag,Digest | head -5
          echo ""
          echo "Frontend images:"
          doctl registry repository list-manifests intelia-frontend --format Tag,Digest | head -5
          echo ""
          echo "LLM images:"
          doctl registry repository list-manifests intelia-llm --format Tag,Digest | head -5

      # Vérification de la disponibilité des images avant déploiement
      - name: Verify images availability
        run: |
          echo "🔍 Vérification des images dans le registry..."
          
          # Vérifier les tags latest spécifiquement
          for repo in intelia-backend intelia-frontend intelia-llm; do
            echo "Vérification de $repo..."
            if doctl registry repository list-manifests $repo --format Tag | grep -q "latest"; then
              echo "✅ $repo:latest trouvée"
              # Obtenir le digest de l'image latest
              digest=$(doctl registry repository list-manifests $repo --format Tag,Digest | grep "latest" | awk '{print $2}' | head -1)
              echo "   Digest: $digest"
            else
              echo "❌ $repo:latest introuvable"
              echo "📋 Tags disponibles pour $repo:"
              doctl registry repository list-manifests $repo --format Tag | head -5
            fi
          done

      # Option A: Déploiement simple avec force rebuild
      - name: Trigger redeploy (use current app sources)
        run: |
          echo "🚀 Déclenchement du redéploiement..."
          echo "App ID: ${{ secrets.DO_APP_ID }}"
          if [ -z "${{ secrets.DO_APP_ID }}" ]; then
            echo "❌ Erreur: DO_APP_ID secret n'est pas configuré"
            echo "👉 Ajoutez le secret DO_APP_ID dans GitHub Settings > Secrets"
            echo "💡 Pour trouver votre App ID:"
            echo "   doctl apps list --format ID,Spec.Name"
            exit 1
          fi
          
          # Attendre un peu pour la propagation du registry
          echo "⏳ Attente de 30s pour la propagation du registry..."
          sleep 30
          
          doctl apps create-deployment ${{ secrets.DO_APP_ID }} --force-rebuild --wait

      # Option B: Déploiement avec tags immuables (recommandé & idempotent)
      # Décommentez si vous préférez piloter par tag SHA plutôt que latest
      # - name: Update spec with immutable image tags and deploy
      #   env:
      #     DO_APP_ID: ${{ secrets.DO_APP_ID }}
      #     GITHUB_SHA: ${{ github.sha }}
      #   run: |
      #     echo "📦 Récupération de la spec..."
      #     doctl apps spec get "$DO_APP_ID" > app.yaml
      #
      #     # Remplacez par les noms EXACTS de vos services dans App Platform:
      #     # ex: backend, frontend, llm
      #     yq -i '(.spec.services[] | select(.name=="backend").image.tag) = env(GITHUB_SHA)' app.yaml
      #     yq -i '(.spec.services[] | select(.name=="frontend").image.tag) = env(GITHUB_SHA)' app.yaml
      #     yq -i '(.spec.services[] | select(.name=="llm").image.tag) = env(GITHUB_SHA)' app.yaml
      #
      #     echo "📝 Mise à jour de l'app spec..."
      #     doctl apps update "$DO_APP_ID" --spec app.yaml
      #     echo "🚀 Déploiement..."
      #     doctl apps create-deployment "$DO_APP_ID" --wait

      - name: Deployment status
        run: |
          echo "✅ Déploiement déclenché avec succès"
          echo "📍 Vérifiez le statut sur: https://cloud.digitalocean.com/apps"

  # Nettoyage optionnel
  cleanup:
    runs-on: ubuntu-latest
    needs: [build-frontend, build-backend, build-llm, deploy]
    if: always() && (needs.build-frontend.result == 'success' || needs.build-backend.result == 'success' || needs.build-llm.result == 'success')
    steps:
      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DOCR_ACCESS_TOKEN }}

      - name: Cleanup old images
        run: |
          echo "🧹 Nettoyage des anciennes images..."
          
          # Nettoyage frontend
          if doctl registry repository list | grep -q "intelia-frontend"; then
            echo "Nettoyage frontend..."
            doctl registry repository list-manifests intelia-frontend --format Digest | head -n -5 | xargs -I {} doctl registry repository delete-manifest intelia-frontend {} --force || true
          fi
          
          # Nettoyage backend
          if doctl registry repository list | grep -q "intelia-backend"; then
            echo "Nettoyage backend..."
            doctl registry repository list-manifests intelia-backend --format Digest | head -n -5 | xargs -I {} doctl registry repository delete-manifest intelia-backend {} --force || true
          fi
          
          # Nettoyage LLM  
          if doctl registry repository list | grep -q "intelia-llm"; then
            echo "Nettoyage LLM..."
            doctl registry repository list-manifests intelia-llm --format Digest | head -n -5 | xargs -I {} doctl registry repository delete-manifest intelia-llm {} --force || true
          fi
          
          # Garbage collection
          doctl registry garbage-collection start ${{ secrets.DOCR_NAME }} --force || echo "Garbage collection déjà en cours"

  # Résumé
  summary:
    runs-on: ubuntu-latest
    needs: [detect-changes, build-frontend, build-backend, build-llm, deploy, cleanup]
    if: always()
    steps:
      - name: Deployment Summary
        run: |
          echo "🚀 DÉPLOIEMENT TERMINÉ"
          echo "===================="
          
          if [[ "${{ needs.detect-changes.outputs.frontend-changed }}" == "true" ]]; then
            echo "Frontend: ${{ needs.build-frontend.result == 'success' && '✅ DÉPLOYÉ' || '❌ ÉCHEC' }}"
          else
            echo "Frontend: ⏸️ INCHANGÉ"
          fi
          
          if [[ "${{ needs.detect-changes.outputs.backend-changed }}" == "true" ]]; then
            echo "Backend: ${{ needs.build-backend.result == 'success' && '✅ DÉPLOYÉ' || '❌ ÉCHEC' }}"
          else
            echo "Backend: ⏸️ INCHANGÉ"
          fi
          
          if [[ "${{ needs.detect-changes.outputs.llm-changed }}" == "true" ]]; then
            echo "LLM: ${{ needs.build-llm.result == 'success' && '✅ DÉPLOYÉ' || '❌ ÉCHEC' }}"
          else
            echo "LLM: ⏸️ INCHANGÉ"
          fi
          
          echo ""
          echo "Déploiement: ${{ needs.deploy.result == 'success' && '✅ RÉUSSI' || '❌ ÉCHEC' }}"