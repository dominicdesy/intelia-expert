# Prometheus configuration for Intelia Expert
# Registry: prometheus
# Scrapes metrics from services via internal networking

global:
  scrape_interval: 60s
  evaluation_interval: 60s
  scrape_timeout: 30s

scrape_configs:
  # Backend /metrics endpoint requires JWT auth (admin only)
  # Not suitable for Prometheus scraping - disabled for now
  # - job_name: intelia-backend
  #   scheme: http
  #   static_configs:
  #     - targets:
  #       - intelia-expert-backend:8080
  #   metrics_path: /metrics
  #   scrape_interval: 15s
  #   scrape_timeout: 10s

  - job_name: intelia-ai-service
    scheme: http
    static_configs:
      - targets:
        - ai-service:8080
    metrics_path: /prometheus
    scrape_interval: 15s
    scrape_timeout: 10s
