# Requirements for Intelia Expert API - Runtime optimisé pour DigitalOcean
# Taille estimée: 1-2GB au lieu de 6GB

# Core FastAPI Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
email-validator==2.1.0.post1

# Database
psycopg2-binary>=2.9
asyncpg>=0.29.0

# Supabase - Compatible with httpx 0.26+
supabase==2.9.1

# Authentication & Security
PyJWT==2.8.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# OpenAI - Compatible version
openai==1.51.0

# HTTP Client - Resolves Supabase/OpenAI compatibility
httpx>=0.26.0,<0.28.0

# RAG System Runtime (optimisé)
sentence-transformers==2.7.0  # Pour charger les embeddings pré-calculés
faiss-cpu==1.8.0              # Pour les recherches vectorielles
numpy==1.26.4                 # Dépendance essentielle
huggingface_hub>=0.24.0       # Pour charger les modèles
safetensors==0.4.3            # Format de modèles optimisé

# SUPPRIMÉ: torch, torchvision (1.8GB économisés)
# SUPPRIMÉ: transformers (500MB économisés) - sentence-transformers suffit
# SUPPRIMÉ: langchain (~700MB économisés) - si vous utilisez FAISS directement

# Document Processing (minimal)
pandas==2.2.2

# Utilities
python-dotenv==1.0.0
aiofiles==23.2.1
toml==0.10.2

# HTTP Requests
requests==2.31.0

# System Monitoring
psutil==5.9.5

# Compatibility
typing-extensions>=4.8.0

# Vector Database & Retry Logic
tenacity>=8.2.2
pinecone>=3.0.0

# OPTIONNEL: Décommentez si nécessaire pour votre RAG
# scikit-learn==1.4.2    # Seulement si vous faites de la classification
# scipy==1.13.0          # Seulement si calculs mathématiques avancés
# tqdm==4.66.1           # Seulement pour les barres de progression
# pillow==10.3.0         # Seulement si traitement d'images