# 🔧 REQUIREMENTS.TXT - CORRECTIONS DES WARNINGS CRITIQUES

# Core FastAPI
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
email-validator==2.1.0.post1

# Supabase
supabase>=2.9.0,<3.0.0

# Auth & Security
PyJWT==2.8.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# OpenAI - 🚨 CORRECTION CRITIQUE: Version compatible
openai==1.51.0
# ANCIEN: openai==1.3.0 (causait l'erreur proxies AsyncClient)

# RAG System - Versions corrigées torch.uint64 + optimisées
torch==2.2.2
torchvision==0.17.2
transformers==4.40.0
sentence-transformers==2.7.0
huggingface_hub==0.23.0
safetensors==0.4.3
faiss-cpu==1.8.0
numpy==1.26.4

# 🔧 NOUVELLES DÉPENDANCES - Correction warnings parseurs
pandas==2.2.2
langchain==0.2.16

# Utils
python-dotenv==1.0.0
aiofiles==23.2.1
toml==0.10.2

# ML libs - Versions compatibles
scikit-learn==1.4.2
scipy==1.13.0
tqdm==4.66.1
pillow==10.3.0

# Requests
requests==2.31.0

# 🎯 CORRECTIONS APPLIQUÉES:
# ✅ openai: 1.3.0 → 1.51.0 (corrige erreur proxies AsyncClient)
# ✅ pandas: Ajouté pour parseurs avancés RAG
# ✅ langchain: Ajouté pour parseurs documents
# ✅ Toutes versions RAG mises à jour pour compatibilité