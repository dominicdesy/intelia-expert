# ============================================================================
# DÉPLOIEMENT main.py CORRIGÉ - VERSION 2.4.0
# ============================================================================

Write-Host "DÉPLOIEMENT main.py CORRIGÉ" -ForegroundColor Green
Write-Host "============================" -ForegroundColor Green

# 1. Backup du fichier actuel
$timestamp = Get-Date -Format "yyyyMMdd_HHmmss"
$backupFile = "backend/app/main.py.backup_$timestamp"

if (Test-Path "backend/app/main.py") {
    Copy-Item "backend/app/main.py" $backupFile
    Write-Host "✅ Backup créé: $backupFile" -ForegroundColor Green
} else {
    Write-Host "⚠️ Fichier main.py non trouvé" -ForegroundColor Yellow
}

# 2. Remplacer par la version corrigée
Write-Host "📝 Remplacement du fichier main.py..." -ForegroundColor Cyan

# Le contenu corrigé sera dans le fichier main_py_complete_fix.py que tu copieras
Write-Host "INSTRUCTION: Copiez le contenu de l'artifact 'main.py - Version Corrigée Complète'" -ForegroundColor Yellow
Write-Host "dans le fichier backend/app/main.py" -ForegroundColor Yellow

# 3. Validation des changements
Write-Host ""
Write-Host "🔍 Validation des changements..." -ForegroundColor Cyan

if (Test-Path "backend/app/main.py") {
    $content = Get-Content "backend/app/